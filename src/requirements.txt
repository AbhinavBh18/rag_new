# ── Core orchestration (LangChain, replacing LlamaIndex) ──────────────
langchain>=0.3.27
langchain-core>=0.3.75
langchain-community>=0.3.27          # PyPDFLoader, BM25Retriever
langchain-text-splitters>=0.3.0
# If you are on LangChain 1.x, the chain constructors moved packages:
#   pip install langchain-classic
# src/pipeline.py imports from langchain_classic first and falls back to
# langchain, so either major version works.

# ── LLM + embeddings ─────────────────────────────────────────────────
langchain-google-genai>=2.0.0
langchain-huggingface>=0.1.2
sentence-transformers>=3.0.0
transformers>=4.44.0                  # token-aware chunk splitting

# ── Vector store (Qdrant, replacing ChromaDB) ────────────────────────
# Pinecone has no Python 3.14 wheel; qdrant-client is pure Python
# (py3-none-any) with 3.14 classified, and runs embedded with no server.
langchain-qdrant>=0.2.0
qdrant-client>=1.19.0

# ── Sparse retrieval ─────────────────────────────────────────────────
rank-bm25>=0.2.2

# ── PDF + app + utils ────────────────────────────────────────────────
pypdf>=4.3.0
streamlit>=1.36.0
python-dotenv>=1.0.0
